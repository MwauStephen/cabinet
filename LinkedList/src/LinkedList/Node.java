package LinkedList;


//we moved the node class to the linkedList class because,
//*its part of the linked list
//linked list must have the node class internally(because its an implementation detail)
